import * as React from 'react';

import SideBar from '../components/side-bar'


export default class SideBarContainer extends React.Component<any, any> {
    render() {
        return (
            <SideBar/>
        )
    }
}
